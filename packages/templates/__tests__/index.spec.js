'use strict';

import { ExpansionPanelActions } from '@material-ui/core';
import { templates } from '../src';

describe('templates', () => {
  it('needs tests', () => {
    ExpansionPanelActions(templates).toBe(templates);
  });
});
